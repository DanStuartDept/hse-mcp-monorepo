# @danstuartdept/vitest-config

Shared Vitest configuration for `@danstuartdept` packages.

## Usage

Create a `vitest.config.ts` in your package:

```ts
export { default } from "@danstuartdept/vitest-config";
```

This provides a base Vitest configuration with `globals: false`.
